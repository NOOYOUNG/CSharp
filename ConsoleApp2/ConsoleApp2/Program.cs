using System;
using System.Collections.Generic;
using System.Linq;
using System.Security.Authentication.ExtendedProtection;
using System.Text;
using System.Threading.Tasks;

namespace ConsoleApp2
{
    internal class Program
    {
        static void Main(string[] args)
        {
            /*
            int x = 10, y = 0;
            Console.WriteLine(10.0 / y);
            Console.WriteLine(x / y);
            

            int x = 10, y = 0;
            try
            {
                Console.WriteLine(x / y);
            }
            catch(Exception e)
            {
                Console.WriteLine(e.Message); // 예외처리 메시지
            }
            

            Console.WriteLine("int maxValue={0}", int.MaxValue);
            int x=int.MaxValue;
            int y = 0;
            y = x + 10;
            Console.WriteLine("int maxValue+10={0}", y);
            

            int x = int.MaxValue;
            int y = 0;
            checked
            {
                try
                {
                    y = x + 10;
                }
                catch (Exception e)
                {
                    Console.WriteLine(e.Message);
                }
            }
            Console.WriteLine("int maxValue+10={0}", y);
            

            string s = " Hello, World! ", t;
            Console.WriteLine(s.CompareTo("abc"));
            Console.WriteLine(String.Concat("Hi~~", s));
            Console.WriteLine(String.Compare("abc",s));
            Console.WriteLine(t = String.Copy(s));

            String[] val = { "apple", "banana", "melon" };
            String result=String.Join(", ", val);
            Console.WriteLine(result);
            

            Console.WriteLine("숫자를 입력하세요");
            string s=Console.ReadLine();
            Console.WriteLine(s);

            int sum = 0;
            string[] v=s.Split(); // 디폴트 구분문자는 공백
            foreach(var i in v)
            {
                sum += int.Parse(i);
            }
            Console.WriteLine("결과는 {0}", sum);
            

            string s1 = "mouse, cow, tiger, rabbit, dragon";
            string s2 = "cow";
            bool b=s1.Contains(s2);
            Console.WriteLine("{0} is in the string '{1} : {2}", s2, s1, b);

            if (b) { 
                int idx=s1.IndexOf(s2);
                if (idx >= 0)
                    Console.WriteLine("{0} begins at index {1}", s2, idx);
            }

            if (s1.IndexOf(s2, StringComparison.CurrentCultureIgnoreCase) >= 0) {
                Console.WriteLine("{0} is in the string {1}", s2, s1);
            }
            */
        }
    }
}
